<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wildcottier.dao.BookMapper">

	<!-- 自己的方法: -->
	<select id="queryBookList" resultType="com.wildcottier.pojo.Book">
		select
		id, name, price, pic, description
		from
		book
	</select>
</mapper>